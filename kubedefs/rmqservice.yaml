apiVersion: v1
kind: Service
metadata:
  name: vpromq01
spec:
  ports:
  - port: 5672       # Service port
    targetPort: vprormq-port  # Pod port (must match containerPort)
    protocol: TCP   # Default (optional)
  selector:
    app: vprormq  # Must match pod labels
  type: ClusterIP