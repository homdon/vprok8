apiVersion: v1
kind: Service
metadata:
  name: vprodb
spec:
  ports:
  - port: 3306       # Service port
    targetPort: vprodb-port  # Pod port (must match containerPort)
    protocol: TCP   # Default (optional)
  selector:
    app: vprodb  # Must match pod labels
  type: ClusterIP